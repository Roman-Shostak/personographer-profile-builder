# Examples

This folder will be populated with reference outputs after the first test runs of the skill.

Planned files:
- `01-researcher-output.json` — example output of Stage 1 (Researcher) on a known subject
- `02-mapper-output.json` — example output of Stage 2 (Editor/Mapper)
- `final-cms-item.json` — example of the created Webflow CMS item structure

These examples will serve as few-shot anchors for the skill, helping Claude calibrate format and tone.
